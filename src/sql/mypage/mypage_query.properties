#myselectList = SELECT D.DEAL_NO, M.MEMBER_ID, P.PRODUCT_NAME, DL.DEALLIST_ADDMESSAGE FROM MEMBER M JOIN DEAL D ON (M.MEMBER_CODE = D.SELLER_CODE) JOIN PRODUCT P ON (P.PRODUCT_NO = D.PRODUCT_NO) JOIN DEALLIST DL ON(DL.DEAL_NO = D.DEAL_NO) WHERE D.CUSTOMER_CODE=? AND DL.DEALLIST_CATEGORY=0
myselectList = SELECT RNUM, DEAL_NO, MEMBER_ID, PRODUCT_NAME, DEALLIST_ADDMESSAGE1 FROM( SELECT ROWNUM RNUM, DEAL_NO, MEMBER_ID, PRODUCT_NAME, DEALLIST_ADDMESSAGE1 FROM (SELECT D.DEAL_NO, M.MEMBER_ID, P.PRODUCT_NAME, DL.DEALLIST_ADDMESSAGE1 FROM MEMBER M JOIN DEAL D ON (M.MEMBER_CODE = D.SELLER_CODE) JOIN PRODUCT P ON (P.PRODUCT_NO = D.PRODUCT_NO) JOIN DEALLIST DL ON(DL.DEAL_NO = D.DEAL_NO) WHERE D.CUSTOMER_CODE=? AND DL.DEALLIST_CATEGORY=0)) WHERE RNUM BETWEEN ? AND ?
cancelUpdate = UPDATE DEALLIST SET DEALLIST_ADDMESSAGE2 =?, DEALLIST_CATEGORY=2 WHERE DEAL_NO=? AND DEALLIST_CATEGORY=0
listCount= SELECT COUNT(*) FROM (SELECT D.DEAL_NO, M.MEMBER_ID, P.PRODUCT_NAME, DL.DEALLIST_ADDMESSAGE FROM MEMBER M JOIN DEAL D ON (M.MEMBER_CODE = D.SELLER_CODE) JOIN PRODUCT P ON (P.PRODUCT_NO = D.PRODUCT_NO) JOIN DEALLIST DL ON(DL.DEAL_NO = D.DEAL_NO)WHERE D.CUSTOMER_CODE=? AND DL.DEALLIST_CATEGORY=0)
CancelselectList =SELECT D.DEAL_NO, M.MEMBER_ID, P.PRODUCT_NAME, DL.DEALLIST_ADDMESSAGE FROM MEMBER M JOIN DEAL D ON (M.MEMBER_CODE = D.SELLER_CODE) JOIN PRODUCT P ON (P.PRODUCT_NO = D.PRODUCT_NO) JOIN DEALLIST DL ON(DL.DEAL_NO = D.DEAL_NO) WHERE D.CUSTOMER_CODE=? AND DL.DEALLIST_CATEGORY=2
cancelDelete= DELETE FROM DEALLIST  WHERE DEAL_NO=? AND DEALLIST_CATEGORY=2
AcceptselectList = SELECT D.DEAL_NO, D.PRODUCT_NO, M.MEMBER_ID, P.PRODUCT_NAME, DL.DEALLIST_ADDMESSAGE FROM MEMBER M JOIN DEAL D ON (M.MEMBER_CODE = D.SELLER_CODE) JOIN PRODUCT P ON (P.PRODUCT_NO = D.PRODUCT_NO) JOIN DEALLIST DL ON(DL.DEAL_NO = D.DEAL_NO) WHERE D.CUSTOMER_CODE=? AND DL.DEALLIST_CATEGORY=1
DealselectList = SELECT D.DEAL_NO, M.MEMBER_ID, P.PRODUCT_NAME, DL.DEALLIST_ADDMESSAGE FROM MEMBER M JOIN DEAL D ON (M.MEMBER_CODE = D.SELLER_CODE) JOIN PRODUCT P ON (P.PRODUCT_NO = D.PRODUCT_NO) JOIN DEALLIST DL ON(DL.DEAL_NO = D.DEAL_NO) WHERE D.CUSTOMER_CODE=? AND DL.DEALLIST_CATEGORY=3


selectWaitingList = SELECT D.DEAL_NO, M.MEMBER_ID, P.PRODUCT_NAME, DL.DEALLIST_ADDMESSAGE FROM MEMBER M JOIN DEAL D ON (M.MEMBER_CODE = D.CUSTOMER_CODE) JOIN PRODUCT P ON (P.PRODUCT_NO = D.PRODUCT_NO) JOIN DEALLIST DL ON(DL.DEAL_NO = D.DEAL_NO) WHERE D.SELLER_CODE=? AND DL.DEALLIST_CATEGORY=0;
cancelUpdateseller = UPDATE DEALLIST SET DEALLIST_ADDMESSAGE =?, DEALLIST_CATEGORY=2 WHERE DEAL_NO=? AND DEALLIST_CATEGORY=0